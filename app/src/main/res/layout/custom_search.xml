<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:layout_marginBottom="5dp"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="10dp"
    android:layout_marginTop="5dp"
    android:background="@android:color/white" >

    <ImageView
        android:id="@+id/search_image_view"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:contentDescription="@string/search_image_view"
        android:padding="@dimen/search_sides_padding"
        android:src="@drawable/ic_action_search" />

    <EditText
        android:id="@+id/search_edit_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toEndOf="@+id/search_image_view"
        android:layout_toRightOf="@+id/search_image_view"
        android:layout_toStartOf="@+id/search_popup_menu"
        android:layout_toLeftOf="@+id/search_popup_menu"
        android:background="@android:color/white"
        android:fontFamily="sans-serif-medium"
        android:hint="@string/search_hint"
        android:singleLine="true" />

    <ImageButton
        android:id="@+id/search_popup_menu"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:src="@drawable/ic_action_overflow"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="@android:color/white"
        android:padding="@dimen/search_sides_padding" />

</RelativeLayout>